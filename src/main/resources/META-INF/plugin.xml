<?xml version="1.0" encoding="UTF-8"?>
<idea-plugin>
    <id>de.brainbo.make-targets</id>
    <name>Make Targets</name>
    <vendor email="heider669@gmail.com" url="https://github.com/brainbo">brainbo</vendor>

    <description><![CDATA[
    <p>Quick access to Makefile targets with a convenient dropdown menu in the toolbar.</p>

    <h2>Features</h2>
    <ul>
        <li>Dropdown menu in the toolbar showing all available Makefile targets</li>
        <li>Displays target descriptions from inline comments (e.g., <code>build: # Build the project</code>)</li>
        <li>Executes targets directly in the integrated terminal</li>
        <li>Configurable Makefile path in Settings</li>
        <li>Works with PhpStorm, IntelliJ IDEA, and other JetBrains IDEs</li>
    </ul>

    <h2>Usage</h2>
    <ol>
        <li>Configure your Makefile path in <b>Settings → Make Targets</b></li>
        <li>Click the dropdown icon in the toolbar or use <b>Tools → Make Targets</b></li>
        <li>Select a target to run it in the terminal</li>
    </ol>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>org.jetbrains.plugins.terminal</depends>
    <extensions defaultExtensionNs="com.intellij">
        <projectService serviceImplementation="de.brainbo.maketargets.MakeTargetsSettingsService"/>
        <projectConfigurable id="de.brainbo.maketargets.settings"
                             instance="de.brainbo.maketargets.MakeTargetsConfigurable"
                             displayName="Make Targets"
                             parentId="project.propCompiler"/>
    </extensions>
    <actions>
        <group id="de.brainbo.maketargets.MakeTargetActionGroup"
               class="de.brainbo.maketargets.MakeTargetActionGroup"
               text="Make Targets"
               description="Run Makefile targets"
               icon="de.brainbo.maketargets.MakeTargetsIcons.MakeTarget"
               popup="true">
            <add-to-group group-id="MainToolbarRight" anchor="last"/>
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </group>
    </actions>
</idea-plugin>